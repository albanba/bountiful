document.getElementById("order").addEventListener("click", displayForm);

function displayForm(){
    const name = document.querySelector("#name").value;
    // document.getElementById('order').textContent = element[0]
    console.log(name)
}